\documentclass[]{standalone}
\usepackage[T1]{fontenc}
\usepackage{tikz}
\usepackage{pgfplots}
\usepackage{amsmath, amsfonts}
\usepackage{xintexpr}
\usetikzlibrary{arrows, shapes.geometric, calc, positioning, decorations.pathreplacing}

\pgfplotsset{scaled y ticks=false}
\pgfplotsset{scaled x ticks=false}
\pgfplotsset{compat=1.16}

\makeatletter
\def\input@path{{./includes/}{./includes/ut/diags/}}
\makeatother

\begin{document}
\begin{tikzpicture}
    % axis options: 'axis', 'loglogaxis'
    \begin{loglogaxis}[
        axis lines = left,
        xlabel = $x$,
        ylabel = {$f(x)$},
        legend pos=south east,
    ]
        % x/W_0(x)
        \addplot[no marks, smooth] gnuplot [
            domain=100:100000000,
            samples=1000,
            color=red,
        ]
        {x/lambertw(x)};
        \addlegendentry{$x(W_0(x))^{-1}$}

        % x
        \addplot[no marks, smooth] gnuplot [
            domain=100:100000000,
            samples=1000,
            color=blue,
        ]
        {0.0638 * x};
        \addlegendentry{$0.0638 \cdot x$}

        % % x
        % \addplot[no marks, smooth] gnuplot [
        %     domain=100:100000000,
        %     samples=1000,
        %     color=green,
        % ]
        % {(x/lambertw(x) - 0.0638 * x)};
        % \addlegendentry{$\frac{x}{W_0(x)} - 0.0638 \cdot x$}
    \end{loglogaxis}
\end{tikzpicture}
\end{document}
