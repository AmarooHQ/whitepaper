# diagrams notes

any file with a name like `*_sag.tex` gets compiled automatically, then you can import to the WP.

## macros

- see simplex-macro.tex
- see tiling-macro.tex

## export svg

add `convert=pdf2svg` to a diagram's `\documentclass`, like: `\documentclass[convert=pdf2svg]{standalone}`
