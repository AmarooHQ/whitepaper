% intro paragraph or something?
% todo mb (intro paragraph)

% something about context with longest PoR chain and axioms?

\todo{Add something about the context regarding axioms and longest PoR chain.}

\subsubsection{Simplex Security}

\label{sec:simplex-security}

Simplexes are secure if attacking a single simplex-chain is as difficult as attacking the whole simplex (the network).

Say the attacker controls $q$ proportion of the network's work-generation capacity (e.g., hash-rate for PoW chains), and honest nodes control $p$ proportion, such that $p+q=1$.
An attacker with $q < p$ can attempt doublespends, and should succeed some of the time (see \autoref{sec:cec-overview}), but never with certainty.
The attacker should only be able to \emph{control} the network's history if $q > p$.

Successfully attacking a single blockchain requires an attacker to publish an alternate chain-segment that the fork rule considers heavier than the corresponding public chain-segment.
Those two segments will have a common ancestor, so the weight of the attacker's segment must be greater than the weight of the public chain-segment (both start at that common ancestor).

Simplex-chains evaluate block-weight as the sum of work done on that block, plus work done on reflecting blocks.
So effecting a doublespend on one simplex-chain requires generating a chain-segment with more total block-weight (including reflections) than the public chain-segment.

It is not viable for the attacker to mine the attacking chain-segment in public (it is self-defeating; see \autoref{sec:dos-and-dags}), so they must mine it in private.
Blocks mined in private will not gain reflections from honest miners on other simplex-chains, so any reflections contributing to the doublespend must be created by the attacker.
The attacker's reflecting blocks (which reflect the attacking chain-segment) on other simplex-chains can be public, but the attacker's task is simpler and not detectable if the attacker mines reflecting blocks in private.\footnote{
    Additionally, if reflecting chains maintain projections as headers-only chains (i.e., construct a main chain via SPV rules), then it should be much harder for an attacker to succeed if reflections are mined in public.
}
There is no viable way for the attacker to prevent honest miners from reflecting the honest chain-segment, including new blocks that extend it, nor to prevent the honest chain-segment from reflecting blocks from other simplex-chains (again, see \autoref{sec:dos-and-dags}).

The honest chain-segment, on the targeted simplex-chain, will not be reflected by the attacker (since that would be self-defeating).
Similarly, the attacker's chain-segment will not be reflected by the honest network, since it's being mined in private.

Let $r$ be the network-wide rate-of-work (hash-rate).
Over some attack duration $d$, on \emph{average} we expect the attacker's chain-segment to weigh $qrd$ and the honest chain-segment to weigh $prd$.
Thus, for the attacker's chain-segment to \emph{reliably} win, it must be that $qrd > prd \implies q > p$.
$\blacksquare$

\subsubsection{Confirmation Equivalence Conjecture}

\label{sec:cec-overview}

\defineTermTex{Confirmation Equivalence Conjecture (CEC)}{
    The conjecture that, when using PoR and appropriately converting work,
    confirmations of reflecting chains
    can be treated as \emph{equivalent} to local confirmations of the same weight.
    See \autoref{eq:cec-extended-hypothesis}
    % Given $q$ and some integer $N$, the probability of
    % an attack succeeding after $C$ confirmations is
    % $\forall a \in \{1,2,\dots,N\}: P^\prime(q; N_1 = a; c = \frac{CN}{a})$
}

The Confirmation Equivalence Conjecture is why PoR works.
If it were false, then PoR could not work, and neither would simplexes.
Additionally, this is the root of UT's $O(c)$ confirmation rate --- the conjecture implies that confirmation rate is proportional to the number of mutually reflecting chains.

Can we test it? If so, how?

Consider a traditional PoW blockchain (e.g., Bitcoin, Eth1, etc).
It's well known that the risk of a doublespend against a particular block is related to the number of \emph{confirmations} that block has.\footnote{
    \citeAHBDSFull
}
However, this is not a linear relationship; rather, it is similar to \emph{exponential decay}.
After the first few confirmations, each additional confirmation reduces the risk of a doublespend by approximately the same factor.\footnote{
    The exact factor depends on the attacker's hash-rate as a proportion of the network's (i.e., $q$).
}
So \emph{security takes \ul{time}}, because confirmations take time.

Now, consider a simplex: the equivalence conjecture says that it doesn't matter whether confirmations come from the \emph{local} chain, or if they come from a \emph{reflecting} chain.
So, a simplex-chain in a 2-chain simplex \emph{should}, after $C$ \emph{local} confirmations, be as secure as a standalone blockchain with $2C$ confirmations.
This is true for larger simplexes, too: \emph{doubling} the size of a simplex means we can \emph{halve} the number of \emph{local} confirmations that we wait for --- the chance of a doublespend succeeding should be approximately the same.
More generally, a simplex-chain in an $N$-chain simplex will, after $C$ \emph{local} confirmations, be as secure as a standalone blockchain after $CN$ confirmations.

This is something we can test!
(See \autoref{sec:por-simulation-experiment} for that.)

As blockchain architects, if the CEC is true, then UT allows us to maintain security by trading \emph{waiting time} for \emph{more simplex-chains!}

\aside{
    If you were looking for an essence of how UT breaks the core conflict of Buterin's Trilemma\dots
}

\subsubsection{Generalizing Doublespends}

\label{sec:cec-draft-refl-work}

With a traditional blockchain, the network has no ``memory'' of work (mining) that has been done which did \emph{not} result in a valid block (i.e., the main chain does not record or account for that work).
Specifically, the network gains no knowledge of how much work has been done \emph{between} blocks.
This is almost self-evident: each block is the \emph{sole} way that work can be \emph{added} to the chain.
Naturally, there is no smaller unit of contributed work than an individual block --- so there's no ``memory''.\footnote{
    We're not concerned with things like \emph{weak blocks} or super-block hybrid designs; though it's possible that doublespend methodology for those designs has some overlap with this section.
}
When an attacker publishes a heavier chain-segment which causes a reorganization, it is \emph{immediately} in the interests of miners to begin building on the attacker's best block.
A heavier chain-segment is \emph{decisively better} in all cases.
That may seem so obvious that it isn't worth mentioning, but it's actually a \emph{specific case} that only works for traditional blockchains.
If there were some ``memory'', it might be worth miners \emph{continuing} to mine their \emph{existing} block, \emph{instead} of reorganizing and building on the attacker's best block.

What kind of ``memory'' could there be?
Are \emph{Proofs of Reflection} a ``memory''?

As it turns out: yes.

Let the total chain-weight of the attacker's best block be called $W_A$, and the total chain-weight of the honest network's best block be called $W_H$.

Consider a miner of a non-DAG simplex-chain (chain $L$) who is working on a \emph{draft block} during an active attack (though, of course, the miner does not know about the attack).
Specifically, let's consider \emph{just before} the attacker publishes their chain-segment.
In the memory pool\footnote{
  The \emph{memory pool} is where unconfirmed transactions (and other things) are stored before they are included in a block.
  Each node has their own memory pool.
  %% --- this must be the case because securely synchronizing memory pools between untrusted parties is as difficult as running a whole blockchain. %% Note: I think this is wrong --- it's only as hard if you have to achieve consensus on which of two conflicting transactions are included.
  A miner typically decides a block's contents by choosing the combination of transactions from their memory pool that results in the largest reward (i.e., transactions with the largest fees).
} of that miner, there will be PoRs, and those PoRs are \emph{only} valid for the \emph{honest} chain-segment (most likely they are for the best known honest block, but don't need to be).
Each $L$ block, in and of itself, only adds $\sim w$ weight to the $L$ chain.
However, if that \emph{draft block} contains $\sim n$ pending PoRs, and each PoR provides $\sim w$ weight on average, then a valid block (created from that draft) will contribute, via PoRs, $\sim nw$ weight to the \emph{honest chain-segment}.
What happens when the attacker's chain-segment is published (along with their reflecting chain-segments for other simplex-chains)?
The miner has a choice: reorganize to the attacker's chain-segment and mine on top of a chain with weight $W_A$, \emph{or} continue mining on the honest chain-segment that weighs $W_H + nw$.

That extra term, $nw$, is the ``memory'' we're looking for, and it makes all the difference.
It is only safe for the attacker to publish their chain-segment \emph{after} $W_A > W_H + nw$ --- only then is it \emph{decisively better} than the honest chain-segment.
If, however, the attacker publishes their chain-segment when $W_H < W_A < W_H + nw$: the best chain-segment for honest miners to mine is \emph{still} the honest chain-segment, not the attacker's!

For non-DAG chains, miners don't actually have to change their behavior at all: their choice is the same, whether they are mining a simplex-chain \emph{or} a traditional chain.
That choice is: \emph{of all possible blocks to mine, which results in the greatest reward?}
If they build on the honest chain-segment, does their resulting block have more total chain-weight than if they were to build on the attacker's chain-segment?

There are some subtleties to consider if chains are DAGs or use GHOST: both chain-segments can be included as ancestors, but which should take priority?
If the weight of PoRs is not taken into account (or applied after evaluating the order of parents), then the attacker's chain-segment takes priority when $W_A > W_H$.
This rule conflicts with what we discussed above, so it must be that parents are ordered based on their chain-weights \emph{including} any PoRs in that block --- the honest chain-segment should take priority when $W_A < W_H + nw$.
Also, note that this is why the weight of PoRs is attributed to the \emph{reflected} block (usually a parent) rather than the block that contains the PoRs.
In essence, the inclusion of this ``memory'' requires that the fork rule take \emph{context} into account --- particularly the context of the descendant block.
This is not a paradox because the fork rule is \emph{only} used to choose the priority chain in the context of some descendant block (even if that block is an invalid draft).


\aside{
    In the context of the longest PoR chain, this property is made somewhat obvious.
}


\subsubsection{Testing the Confirmation Equivalence Conjecture}

\label{sec:por-simulation-experiment}

Let's test the Confirmation Equivalence Conjecture.

\subsubsubsection{Hypothesis}

The hypothesis is that: doublespends against an \emph{individual simplex-chain} in an $N$-chain simplex after $\mathcal{C}$ \emph{local} confirmations are \emph{as hard} as doublespends against a traditional blockchain after $\mathcal{C}\cdot N$ confirmations.

\pz{c and m have not been defined. Also, we use the same letter for local computation power and confirmations.}
First, define $P(q; c)$, where $c$ is the number of confirmations and $q$ is the proportion of global hash rate controlled by the attacker, to be the function that returns the probability of an attempted doublespend succeeding on a traditional blockchain according to Rosenfeld's analytical solution\footnote{
    \citeAHBDSFull
}:
\pz{
  Unclear if $\{1, 2, \dots\}$ is finite or infinite.\\
  -------\\
  Chris: A few notes on the above comments:\\
$m$ is defined as part of the $\Sigma$, counting from $m=0$ to $c$;\\
The $\{1,2,\dots\}$ I read as unbounded unless a specific bound is mentioned, quite typical in mathematical proofs. Since the parameter $c$ is the number of confirmations and can be as high as it wishes. We could say it as $\mathbb{N}^+$?}


\begin{equation}
  \forall c \in \{1, 2, \dots\}: P(q; c) = \begin{cases}
        1 - \sum_{m=0}^c \binom{m+c-1}{m}(p^c q^m - p^m q^c), & \text{if } q < p \\
        1, & \text{otherwise}
    \end{cases}
    \label{eq:rosenfeld-analytical-p}
\end{equation}

Next, we will declare a new function, $P^\prime$, and assume that \emph{it exists and is measurable}.
Particularly: $P^\prime(q; c = \mathcal{C}; N_1 = N)$ is the probability of a doublespend attack succeeding against a simplex with $N$ simplex-chains after $\mathcal{C}$ confirmations, given an attacker with $q$ proportion of the network hash-rate.
We do not know the definition of this function, but we can measure its output.

As a base case, it is taken that:
\begin{equation}
    P(q; c) \equiv P^\prime(q; c; N_1 = 1)
    \label{eq:cec-base-case}
\end{equation}
Note that a simplex of 1 chain (the 0-simplex) \emph{is} a traditional blockchain.
So we can take Bitcoin for example: the probability of an attack succeeding after 6 confirmations is given by $P(q; c = 6) \approx P^\prime(q; c = 6; N_1 = 1)$.

One prediction of the hypothesis is that the probability of a successful attack against a simplex with $N$ simplex-chains after $\mathcal{C}$ confirmations is equal to that of a successful attack on a traditional chain after $\mathcal{C}\cdot N$ confirmations.
That is:
\begin{equation}
  P^\prime(q; c = \mathcal{C}N; N_1 = 1) \approx P^\prime(q; c = \mathcal{C}; N_1 = N)
    \label{eq:por-sim-hypothesis}
\end{equation}
\pz{Variable $a$ used in footnote but only defined a posteriori.}
In fact, we can take this further.
The relationship that exists is not only at the extremes, but between them too\footnote{
  Since \nicefrac{$\mathcal{C}$N}{a} in \autoref{eq:cec-extended-hypothesis} is not necessarily an integer, we'll assume there exists a valid generalization of Rosenfeld's solution for $c \in \mathbb{R}_{>0}$.
} --- it must be if confirmations are equivalent.
This is the \emph{extended} CEC, where $a$ is the number of simplex-chains:
% 1 \le x \le N; x \in \{a_i, b_i\}
% , and $1 \le a_i \le N$, and $1 \le b_i \le N$
\begin{equation}
    \begin{split}
        %P\biggl(q; N_1 = a; c = \frac{CN}{a}\biggr) \approx P\biggl(q; N_1 = b; c = \frac{CN}{b}\biggr)
      \forall a \in \{1,\dots,N\}: P^\prime\biggl(q; c = \frac{\mathcal{C}N}{a}; N_1 = a\biggr) &\text{ is approximately constant}
        \label{eq:cec-extended-hypothesis} \\
        \implies P^\prime\biggl(q; c = \frac{\mathcal{C}N}{a}; N_1 = a\biggr) &\approx P(q; c = \mathcal{C}N)
        % \label{eq:cec-approx-rosenfeld}
    \end{split}
\end{equation}

Notice that we don't need to know a closed form of $P^\prime(q, c, N_1)$ to test the CEC: all cases \emph{that we can measure} are equivalent to some \emph{other, traditional} case, and we can convert between them.\footnote{
    This is not strictly true: Rosenfeld's solution requires an integer value for $c$.
    This is due to his solution assuming a static difficulty and the longest-chain fork rule; the \emph{construction} is consistent with Bitcoin (provided the difficulty doesn't adjust), but not with chains that recalculate difficulty every block.
    Additionally, when using \emph{work} instead of \emph{height} as the measure of an attack's success, fractional values of $c$ have real meaning --- especially with a reactive DAA and $N_1 \gg 1$.
    Both factors turn out to be unproblematic for us, though.
    Rosenfeld's solution has reach beyond those assumptions and observed results converge with it for larger values of $\text{DAA}_N$.
}
This is the basis of the method used to test this hypothesis.

If \autoref{eq:cec-extended-hypothesis} is correct, it indicates that there exists a \emph{generalization} of traditional blockchains (which have $N_1 = 1$);
particularly, that there is \emph{another dimension} ($N_1$; the size of the simplex) which is geometrically related to the first dimension, $c$.

Note: this experiment does \emph{not} test the conversion of chain-work --- the same hashing algorithm is used for each simplex-chain and no conversion of work takes place.


% \subsubsubsection{Scope and Constraints}

% This experiment is not intended to be comprehensive --- many significant real-world factors are basically ignored: e.g., block propagation latency, transaction processing overhead, geographic latency, bandwidth constraints, etc.

% The scope is \emph{only} intended to cover PoR as a consensus mechanism --- particularly with respect to doublespends.

% Since we want to simulate \emph{specific attacks} we can guarantee that the honest nodes and attacking nodes will only behave in \emph{specific ways}.
% This means, that blocks will always be valid: e.g., neither honest nor attacking nodes will generate blocks with invalid reflections.


% This constraint means there are many ways to simplify implementation --- for example, reflected blocks are not checked against the history of a blockchain.
% These simplifications would render a \emph{production} blockchain insecure.
% However, the simplifications are okay in this case since we \emph{expect} near-identical behavior from well functioning networks, and the simplifications are otherwise independent of what we are testing --- the consensus mechanism.
% In essence, we want to simulate a \emph{specific} attack, so the implementation doesn't need to be hardened because we know that the attacker will \emph{only} attack in the specific way we care about.

% So, while it is \emph{possible} that a poor implementation of a simplex could compromise its security, we'll assume these kinds of design flaws are \emph{in principle} avoidable.

% \todoDraftOnly{Test chains of different hash-rates + anything else to cover bases re CEC}



\subsubsubsection{Method}

I have implemented a simulation of a blockchain network that is suitable for this experiment.
Particularly:

\begin{itemize}
    \item The implementation is modular.
    It supports multiple: attack strategies; blockchain data structures; hashing algorithms (for PoW); and fork rules.
    \item An executable is produced that can run a single instance of a simulated attack, and takes a variety of parameters.
    (This is looped through via bash script to obtain CSVs of the results.)
    \item The implementation supports both traditional blockchains ($N_1 = 1$) and PoR/simplexes.
    \item It is fast.
\end{itemize}

The source code for this model blockchain network is contained in the \href{https://github.com/AmarooHQ/whitepaper/tree/master/experiments/por-sim-rs}{\texttt{/experiments/por-sim-rs/}} folder of \href{https://github.com/AmarooHQ/whitepaper}{this paper's repository}.

While the simulation supports blockchains with single-parent blocks and a ``longest chain'' fork rule, these were not used in this experiment.
Rather, multi-parent blocks were used (which simulates both GHOST and block-DAGs), along with the weight-based fork rule --- these are required by UT.

\begin{table}[H]
    \centering
    \caption[
        Table of parameters necessary to generate the main CEC experimental results.
    ]{
        Table of parameters necessary to generate the main CEC experimental results.
        Only those parameters marked as variable were altered while generating the main results.
        Some parameters are omitted as they are not directly relevant to this experiment.
    }
    \begin{tabular}{llp{0.64\linewidth}}
        \toprule
        Parameter & Variable & Significance \\
        \midrule
        $q$ (ratio) & Yes & Attacker's proportion of the global hash-rate. \\
        $c$ (confirmations) & Yes & Work-equivalent of `$c$ local confirmations' waiting time. \\
        $N_1$ (chains) & Yes & The number simplex-chains ($N_1 = 1$ for traditional blockchains). \\
        ${B_f}^{-1}$ (ticks) & No & Artificial units of time used by the DAA. \\
        $\text{DAA}_N$ (blocks) & No & The number of blocks over which the DAA operates. \\
        $H$ (hashes) & No & The network-wide average hashes per chain per tick. \\
        \bottomrule
    \end{tabular}
\end{table}

% with attack simulations wherein each chain uses PoW and has a fully functioning fork rule.
% This design is modular, such that it supports different fork rules, different block data structures and ordering algorithms (including multiple parents), different attack strategies.
% The program also accepts a variety of parameters --- both for the simulation itself, the individual chains, and for each attack strategy.

% As we can optimize this model, we can simulate many attacks quickly; so we can efficiently make statistically significant measurements.

When choosing values for ${B_f}^{-1}$ and $H$, we want them to be low enough for the simulation to be efficient, but large enough to ensure that there are no issues with limits-of-accuracy.
To this end, both were chosen to be 75.
This means that, prior to the doublespend attack beginning, the average difficulty is around 5625.

We also want to consider $\text{DAA}_N$ --- but there isn't an issue of \emph{precision} in this case.
The DAA used\footnote{
    The simulation uses the algorithm named \textsc{DAA-2} from \citeDaaTwoLink{}.
} adjusts every block, so lower values of $\text{DAA}_N$ mean the DAA is more reactive to a drop in hash-rate.
If the DAA is more reactive, then attackers need to do \emph{a little bit more work} since, on average, each of their blocks will weigh less than an honest block at an equivalent height.
If the attacker's blocks (on average) weigh less, then the fork rule will favor an honest chain-segment (which has, on average, heavier blocks) and the attacker will need at least one \emph{extra} block for their chain-segment be favored.
The only way to overcome this, for shorter attacks or attacks against smaller simplexes, is for the attacker to be \emph{a little bit luckier than usual}.
(The effect is less significant with long attacks or attacks against larger simplexes since $P^\prime \to 0$ anyway.)

\aside{
    This explanation --- that a smaller $\text{DAA}_N$ means the attacker needs to be luckier --- was not something I expected.
    It is somewhat speculative, and is included here to explain why smaller simplexes (3 to 10 chains) appear to have better security properties than the analytical solution predicts, and why we should prefer larger values of $\text{DAA}_N$ if we want to match expected results.
}

In essence, a shorter $\text{DAA}_N$ means that $P^\prime$ approaches zero faster.
Since we want to \emph{match} analytical results, we want a \emph{larger} value of $\text{DAA}_N$ for this experiment --- this is analogous to an assumption of Rosenfeld's solution: \emph{constant difficulty}.

So that we match the analytical solution's assumptions, a value of $\text{DAA}_N = 500$ was chosen.
The most important thing is that $\text{DAA}_N \gg c$, particularly regarding traditional chains.\footnote{
    The differential effect seems to diminish as an attack goes on, so $\text{DAA}_N$ is particularly important for \emph{lower} values of $c$.
    $\text{DAA}_N \gg c$ matters when c is small, but not so much when $c$ is large.
    In other words: if the doublespend occurs over 600 blocks, the value of $\text{DAA}_N$ doesn't matter much; but if the doublespend occurs over 20 blocks, then the value of $\text{DAA}_N$ is a significant variable.
}

\aside{
    In practice, we should seriously consider smaller values of $\text{DAA}_N$ for the security benefit.
}

\pz{Variable n has already been defined.}
With regards to the other values --- $q$, $c$, and $N_1$ --- they will be varied as part of the experiment so that we can generate each data-point.
For each data-point, we'll gather at least 9,000 samples (this is indicated by ``$n \ge 9000$'' in these figures' legends).

The goal is to generate multiple data series that the CEC predicts to align.
To that end, the functions in \autoref{table:cec-functions-to-graph} are measured and graphed on the same axes.
If the CEC is true then we expect: $f_1(x) \approx f_2(x) \approx f_3(x) \approx f_4(x) \approx f_5(x)$.
% For $q \in \{0.4, 0.44\}$ and $C = 5$:
% \begin{itemize}
%     \item $f_1(x) = P(q; c = Cx)$ --- Rosenfeld's analytical solution
%     \item $f_2(x) = P^\prime(q; c = Cx; N_1 = 1)$ --- Simulated doublespend against a traditional chain
%     \item $f_3(x) = P^\prime(q; c = C; N_1 = x)$ --- Simulated doublespend against a simplex
%     \item $f_4(x) = P^\prime(q; c = 2C; N_1 = x/2)$ --- Simulated doublespend against a simplex
%     \item $f_5(x) = P^\prime(q; c = 4C; N_1 = x/4)$ --- Simulated doublespend against a simplex
% \end{itemize}
% If the CEC is true then we expect: $f_1(x) \approx f_2(x) \approx f_3(x) \approx f_4(x) \approx f_5(x)$.
\begin{table}[H]
    \centering
    \caption[
        Functions that will be simultaneously graphed to test the CEC.
    ]{
        Functions that will be simultaneously graphed to test the CEC.
        The main results are generated for $q \in \{0.4, 0.44\}$ and $C = 5$.
        The CEC predicts: $f_1(x) \approx f_2(x) \approx f_3(x) \approx f_4(x) \approx f_5(x)$.
    }
    \label{table:cec-functions-to-graph}
    \begin{tabular}{ll}
        \toprule
        Function & Description \\
        \midrule
        $f_1(x) = P(q; c = Cx)$ & Baseline: Rosenfeld's analytical solution (calculated) \\
        $f_2(x) = P^\prime(q; c = Cx; N_1 = 1)$ & Simulated doublespend against a traditional chain \\
        $f_3(x) = P^\prime(q; c = C; N_1 = x)$ & Simulated doublespend against a simplex \\
        $f_4(x) = P^\prime(q; c = 2C; N_1 = x/2)$ & Simulated doublespend against a simplex (via CEC) \\
        $f_5(x) = P^\prime(q; c = 4C; N_1 = x/4)$ & Simulated doublespend against a simplex (via CEC) \\
        \bottomrule
    \end{tabular}
\end{table}




\subsubsubsection{Error Correction Iteration}

\pz{Having a probabilistic model and proving it empirically would be far more convincing than trying to make the experiments fit the hypothesis.}
Following the initial implementation of PoR in the simulator, results were promising but did not align with the theoretical prediction (based on Rosenfeld's analytical derivation of $P(q; c)$).
At lower values of $c$ and/or $N_1$, the results suggested that the simulation was \emph{more} secure than expected.
At higher values of $N_1$, $P^\prime$ did not appear to approach 0, instead approaching $\sim 0.05$.

Given the nature of writing a simulation (compared to writing a production blockchain), many simplifications were made.
Some of these simplifications shouldn't matter, but some might.
How do we know if a given simplification (or implementation detail) introduced errors that were responsible for unexpected results?

In this case, sources of error were brainstormed, and evaluated in turn based on their potential to explain the largest sources of error.
Of those, 4 major sources of error were corrected, which incrementally brought the simulation results closer and closer to the expected results (based on the CEC prediction).
The error correction steps (including interim results) are briefly documented in \autoref{sec:appendix-cec-extra}.
Those corrections were:

\begin{enumerate}
    % \item Implementing the \texttt{DoubleSpendWork} attack strategy
    \item Accounting for draft reflected work (explained in \autoref{sec:cec-draft-refl-work})
    \item Randomizing hash-rates over the simplex ($p + q = 1$ is globally maintained)
    \item Implementing the attacker's ``Bonus Block''
    \item Increasing the number of blocks over which the DAA operates ($\text{DAA}_N = 100 \to 500$)
\end{enumerate}

Many possible sources of error were ruled out, too, like: choice of hashing algorithm (\texttt{xxh3}); values of ${B_f}^{-1}$, $H$, and other parameters; and implementation details like omitting certain validation steps.

% \begin{itemize}
%     \item Hashing algorithm --- \texttt{xxh3} is used and produces results consistent with \texttt{blake3}.
%     \item Values of ${B_f}^{-1}$ and $H$ --- both have enough precision; particularly $Hq$ should result in an integer.
%     \item Several implementation details --- e.g., how much validation is done on blocks.
% \end{itemize}

Changing the simulation based on early results is dangerous though --- why should we believe these results are valid?
The answer is simple: all error corrections were motivated by \emph{making the simulation more like a real blockchain}.
No magic numbers were introduced, no values were scaled arbitrarily, simulations were not repeated to get better results, etc.
Since we \emph{know} that a simulation will naturally include simplifying assumptions (and omitted implementation details), we should expect that some of these simplifications might be significant.
So focusing on, and correcting, those simplifications does not compromise the method.
Rather, the fact that \emph{these} corrections were the significant ones is consistent with the idea that \emph{the simulation is consistent with real-world blockchain networks}; that the simulation \emph{works}.



\todoDraftOnly{assumption: data is available: i.e., if a block is reflected then it is known. this forces the attacker to mine in private. that's an assumption, too: the miner mines in private.}

\todoDraftOnly{Low values of $c$ much more secure? or mb limitation of simulation. possible explanation: draft refl work that already exists but that the attacker cannot use?}

\todoDraftOnly{attacker's draft refl work?}

\subsubsubsection{Validating the Model}

\begin{figure}
    \centering
    \includegraphics[max width=\linewidth, max height=0.5\textheight]{simulation/_results_trad_validation_9000_t=5_daa=500.pdf}
    \caption[
        A comparison of the measured probability of a successful doublespend against a traditional blockchain compared with Rosenfeld's analytical solution.
    ]{
        A comparison of the measured probability of a successful doublespend against a traditional blockchain compared with Rosenfeld's analytical solution, for $q \in \{0.4, 0.44\}$.
        That the measured results converge with the analytical solution is taken to be validation of the simulation --- i.e., it \emph{works} and is \emph{useful} in the context of this experiment.
    }
    \label{fig:simulation-trad-only}
\end{figure}

At a minimum, we should expect the simulation to faithfully replicate doublespends against \emph{traditional} chains --- i.e., $f_1(x) \approx f_2(x)$.
Results of this, for $q \in \{0.4, 0.44\}$, are shown in \autoref{fig:simulation-trad-only}.

\autoref{fig:simulation-trad-only} shows that the simulation \emph{does} align with Rosenfeld's solution, especially at values of $q \lesssim 0.40$.
It is consistent with \autoref{eq:cec-base-case}.
As $q$ increases, statistical artifacts and other inaccuracies \emph{do} become more significant, but not enough to compromise the results --- the largest differences (for $q=0.44$ and $c=5$) are around 50 parts per thousand (\autoref{fig:simulation-r1-single-simplex}) and typically less than this.
There are more significant differences at $q=0.48$, however this is close to limiting threshold of blockchain security in general ($q<0.5$), so we should expect larger error in the simulation results.
Such differences are taken to be \emph{non-critical}, i.e., they do not compromise the results.




\subsubsubsection{Results}

\label{sec:cec-experiment-results}

In this section we will only look at results for $q \in \{0.4, 0.44\}$, though results for $q=0.48$ were also generated (among other combinations of parameters).
Results that are omitted from this section are available in \autoref{sec:appendix-cec-extra}.

First, do results of the simplest version of the CEC match predictions?
\autoref{fig:simulation-r1-single-simplex} graphs $f_1, f_2, f_3$ and the results are consistent with predictions (\autoref{eq:por-sim-hypothesis}).

\begin{figure}
    \centering
    \includegraphics[max width=\linewidth, max height=0.4\textheight]{simulation/_results_trad_vs_1simplex_9000_qs=0.40-0.44_t=5_daa=500.pdf}
    \caption[]{
        A comparison of the measured probabilities, for $q \in \{0.4, 0.44\}$, of a successful doublespend between: a traditional blockchain, a simplex, and Rosenfeld's analytical solution.
        That the measured results for a simplex (converted via the CEC) converge with the other results is taken to be evidence of $O(n)$ security and that the CEC is non-refuted.
    }
    \label{fig:simulation-r1-single-simplex}
\end{figure}

While it appears that simplexes (for $q=0.44, c=5, N_1 \in [3,10]$, at least) are slightly more secure than expected, this effect diminishes as $N_1$ increases.
As we are about to see, it also diminishes as $c$ increases.
For this experiment, it is taken to be a \emph{non-critical} error.

Second, what about the \emph{general case?}
Does the extended CEC hold true, also?
\autoref{fig:simulation-r2-cec-ext} graphs $f_1, f_2, f_3, f_4, f_5$ and these results are also consistent with predictions (\autoref{eq:cec-extended-hypothesis}).

\begin{figure}[htb]
    \centering
    \includegraphics[max width=\linewidth, max height=0.6\textheight]{simulation/_results_cec_9000_qs=0.40-0.44_t=5_daa=500.pdf}
    \caption[]{
        A comparison of the measured probabilities, for $q \in \{0.4, 0.44\}$, of a successful doublespend between: a traditional blockchain and multiple simplexes converted via the CEC.
        That the results converge as predicted is taken to mean that the extended CEC is non-refuted.
    }
    \label{fig:simulation-r2-cec-ext}
\end{figure}

% \FloatBarrier

\subsubsubsection{Conclusions}

\pz{The first part of the footnote could be moved as a disclaimer to the beginning of the section. The lack of refuting evidence is also due to the fact that it is not published yet.}
These results are, for the purpose of this paper, taken to indicate the following statements are true\footnote{
    Experimental results never prove that something is correct, however, they are criticisms of \emph{competing} explanations.
    For example: we see that these experimental results \emph{converge} as expected.
    This fact is NOT evidence that UT is secure, or that PoR works, or that the CEC is right.
    But, it is a \emph{pre-emptive criticism} of ideas like: \emph{The simulation does not work}, or \emph{PoR is broken}, or \emph{Simplexes aren't secure}, or \emph{Sharing security between blockchains is impossible}.
    A decisive criticism of UT/PoR/CEC must therefore explain how these results are wrong --- the explanation needs to include a \emph{refutation} of these results.
    Such a refutation could be an explanation of how: the implementation was flawed, the methodology was flawed, or that UT cannot be effectively implemented as a production blockchain.
    No such explanations are currently known.
    In the \emph{absence} of such criticisms, it is reasonable to work under the assumption that UT is secure, since \emph{PoR works} and \emph{the CEC is true} are currently (to my knowledge) the only viable explanation for these results.
}.
\begin{enumerate}
    \item Sharing PoW security between blockchains without merged mining is possible.
    \item PoR \emph{works} as claimed when implemented correctly.
    \item The \emph{Confirmation Equivalence Conjecture} is, \emph{generally and in principle}, true.
    \item Simplexes are secure against a minority attacker and are thus $O(n)$ secure (assuming no relevant bottlenecks).
    % more stuff that's useful for UT's case?
\end{enumerate}

% Additionally, the following observations are made:

% \begin{enumerate}
%     \item An attacker whose hash-rate is \emph{uniformly} distributed over a simplex is at a disadvantage compared to an attacker whose hash-rate is \emph{randomly} distributed.
%     \item Decreasing $\text{DAA}_N$ increases the security of both simplexes and traditional blockchains --- except for ``Longest Chain'' fork rules.
%     \item An attacker that waits to generate a block on a target chain is at a decisive advantage compared to one that does not.
%     \item Rosenfeld's closed form solution is consistent with empirical results.
% \end{enumerate}

% \subsubsubsection{Experimental Limitations}

% \todo{
%     seems like simplexes might have trouble getting below 1\% probability of successful attack --- like large $N_1$ has $\sim$diminishing returns.
%     This effect disappears after more local confirmations tho.
% }

% \todo{por sim - limits}

% \begin{itemize}
%     \item could be insecure for reasons we didn't test for
%     \item those ignored factors could be decisive
%     \item insufficient breadth of attack parameters
%     \item didn't come up with a good strategy (mb there's a better one than mining in private)
%     \item confirmation bias in implementation --- might have missed something
%     \item does it work for simplexes that have PoS/PoA chains? not tested for
%     \item some validation type things not done
%     \item assumed that there's at least 1 local confirmation --- no claims about 0-conf stuff
% \end{itemize}

% sources of error

% \begin{itemize}
%     \item attacker q (e.g., 0.44) divisibility into B\_PERIOD and/or HR\_PER\_CHAIN (should not be an issue w/ these tests, nor w/ random HR distribution) (exp21 --- high res: see \texttt{png/highres\_vs\_std*.png})

%     \item small HR\_PER\_CHAIN --- only 50-100 hashes every tick (exp14 --- more is better but results at HR=50 still consistent with HR=100; answered w exp21)

%     \item small values $\to$ limits of accuracy mb? (in other ways, not just attacker q divisibility). exp 14 indicates not really; also exp21

%     \item using xxh3 instead of a crypto secure hashing alg like blake3 (answered: exp16/17; not an issue; also exp21)

%     \item not giving the attacker a 'bonus block' (i.e. they only start after having mined a block in private)

%     \item not waiting long enough for doublespends to resolve --- since we end before $t=\infty$ it is the case we cut off \emph{some} doublespends that might be successful --- is it significant? (answered: exp18/19/20; situation where it is significant: \texttt{png/atk\_length\_comparison\_q=0.48\_t=5.png}; not significant with $q \le 0.4$ or $C \ge 20$)

%     \item does weighted chain favor lower difficulty? if priv difficulty is 0.75 * pub difficulty, then is there an expectation that priv chain can find 2x PoWs a bit faster, so statistically there's usually a point where priv chain is ahead (more PoWs but worth less)?

%     \item WRT some bonus block simulations having P above theoretical (e.g., q=0.4 at C=5):
%             perhaps this is to do with number of ticks. particularly: there's mb a point where refls are arriving every few ticks --- does the attacker have an advantage when this happens? Like for N < 5 it's fine, and for N > 30 its fine

%     \item error in implementation? (note: mb should see discrepancies between diff crypto systems / attack strats if that were the case --- depends on the impl error)

%     \item theory/CEC wrong? --- nonspecific crit but possible

%     \item allowing invalid blocks / txs / refls --- either b/c of simplifications or b/c of misunderstanding of reality?
% \end{itemize}

% \subsubsubsection{Remarks}

% \aside{
%     I estimate that my CPUs alone used $\sim 100$ million J to obtain the results for this experiment.
%     I estimate also estimate that, in that time, my body used $\sim 50$ million J (assuming 100 W per day at 50\% utilization for 12 days).
% }
