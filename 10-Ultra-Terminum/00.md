---
# title: Nōs Ultrā Terminum Ultimum Imus, Nunc
title: Nos Ultra Terminum Ultimum Imus, Nunc

author:
  - Max Kaye \textit{(Amaroo)}
  # - Max Kaye aka. ``XertroV'' \textit{(Amaroo)}
  #  # --- \textit{Philosopher (\href{https://criticalfallibilism.com/}{Critical Fallibilism})}

date: >
    \begin{center}
      The Amaroo Consensus Strategy Whitepaper
    \end{center}

# NB: these get overwritten by the Makefile -- but these are the defaults
geometry: left=3cm,right=3cm,top=3cm,bottom=3cm
papersize: a4
# classoption:
#   - twoside

# colorlinks: true
# linkcolor: cyan!60!black
# filecolor: cyan!60!black
# citecolor: cyan!60!black
# urlcolor: maxsColor

# NOTE: Comments using `%` (or, rather, `%%`) in header-includes BOTH cannot exceed a line length of 60 AND cannot be on the line immediately following another comment. If there are comments like this, then pandoc will merge the lines and then split this new line at like column 65! This kills the latex.

# In general, the only SAFE way to use comments is with blank lines above AND below them.

header-includes: |
    \usepackage[utf8]{inputenc}
    \usepackage{comment}
    \usepackage[lastpage,user]{zref}
    \usepackage{fancyhdr}
    \usepackage{amsmath,amsfonts}
    \usepackage[dvipsnames]{xcolor}
    \usepackage{mathtools}
    \usepackage{mdframed}
    \usepackage[skins,breakable,most]{tcolorbox}
    \usepackage[yyyymmdd,hhmmss]{datetime}
    \usepackage{nicefrac}
    \usepackage{epigraph}
    \usepackage{verbatim}
    \usepackage{subcaption}
    \usepackage{float}
    \usepackage{xhfill}
    \usepackage{varwidth}
    \usepackage{pdfpages}
    \usepackage{algpseudocode}
    \usepackage{algorithm}
    \usepackage[automake,toc,nogroupskip]{glossaries}
    \usepackage[titles]{tocloft}
    \usepackage[fit]{truncate}
    \usepackage{wallpaper}
    \usepackage[section]{placeins}
    \usepackage[export]{adjustbox}
    \usepackage{ifthen}
    \usepackage{array}
    \usepackage{tabularx}
    \usepackage{ctable}
    \usepackage{trimspaces}
    \usepackage{cancel}
    \usepackage[all,cmtip]{xy}
    \usepackage{morewrites}
    \usepackage{xparse}
    \usepackage{forest}
    \usepackage{mathdots}
    %% for labeling lists
    \usepackage{enumitem}
    %% use cref not autoref for items
    \usepackage[nameinlink]{cleveref}

    \usepackage{marginnote}


    \begin{comment}
    Leave this stuff in the preamble -- will probably break stuff if moved to includes.

    Note: the directory "output" is hardcoded in the texcount cmd.
    \end{comment}

    \immediate\write18{texcount output/\jobname.tex -merge -sum -0 -out=output/\jobname.sum}
    \newcommand\wordcount{\input{output/\jobname.sum}}

    \makeatletter
    \def\input@path{{./}{./includes/}{./includes/ut/}{./includes/ut/content/}{./includes/ut/diags/}}
    \makeatother

    \graphicspath{{./}{./includes/}{./includes/ut/diags/}{./experiments/tiling/}{./includes/ut/}}

    \newcommand\preparedfor{}
    \begin{comment}
    \renewcommand\preparedfor{\\ \scriptsize{Prepared for NAME. \\ Not for distribution.}}
    \end{comment}

    \providecommand*\ErrorOnReleaseIfTODOs{no}
    \providecommand*\BuildMode{release}

    %% BEGIN ### DRAFT

    \renewcommand*\ErrorOnReleaseIfTODOs{no}
    \renewcommand*\BuildMode{draft}

    %% END ### DRAFT

    %% BEGIN ### RELEASE

    \newcommand{\currentReleaseLabel}{--RELEASE-LABEL--}

    %% END ### RELEASE

    %% does nothing atm
    \newcommand\darkMode{false}

    \begin{comment}
    Anything else should go in an includes file. Like these:
    \end{comment}

    \input{includes/ut/00-preamble/000-all-preambles.tex}

#  note: the directory "output" is hardcoded in the texcount cmd above.

abstract: |
    \thispagestyle{firststyle}
    \input{includes/ut/00-abstract.tex}

# this stuff goes before the main document starts

include-before: |
    %% RELEASE-LINT-START

    %% BEGIN ### RELEASE

    %% bottom center of title page

    \AddToShipoutPictureBG*{%
      \begin{tikzpicture}[overlay]
        \fill [white] (0,0) rectangle (\paperwidth,\paperheight);
      \end{tikzpicture}}
    \setlength\wpXoffset{-0.00\paperwidth}
    %%\setlength\wpYoffset{-0.38\paperheight}
    %%\ThisCenterWallPaper{0.95}{tiling_alt_s7_d6_bw_sag}

    \setlength\wpYoffset{-0.3875\paperheight}
    \ThisCenterWallPaper{0.975}{tiling_cover_noextrachild_sag}

    %% below works w/ d=5
    %%\setlength\wpYoffset{-0.385\paperheight}
    %%\setlength\wpYoffset{-0.390\paperheight}
    %%\ThisCenterWallPaper{0.95}{tiling_cover_v4_s7_d6_bw_sag}


    \vspace*{\fill}
    \begin{center}
    %%\includegraphics[height=\measurepage,width=\textwidth,keepaspectratio]{tiling_cover_sag}
    \end{center}
    \vspace*{\fill}

    %% epigraph page (commented)

    \begin{comment}
    \newpage

    \vspace*{\fill}
    \epigraph{
        \textit{
            A thousand ages in thy sight \\
            \hspace{0.4em} Are like an evening gone; \\
            Short as the watch that ends the night \\
            \hspace{0.4em} Before the rising sun.
        }
    }{}
    \vspace*{\fill}
    \end{comment}

    %% END ### RELEASE

    %% BEGIN ### DRAFT

    \newpage

    \input{includes/ut/01-draft-msg.tex}
    \listoftodo
    \listofcomment

    %% END ### DRAFT

    %% BEGIN ### RELEASE

    \newpage

    \begin{comment}
    NB: the END lint-section tag is in 01-todos.md.
    The TOC commands are injected by pandoc after this point.
    \end{comment}
---
