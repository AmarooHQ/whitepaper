\begin{comment}
Terminus Est
\end{comment}

\vspace{1.3em}
\noindent
\xrfill[0.7ex]{0.5pt}
\vspace{0em}

# Ultra Terminum

\todo{note: this section contains the innovations behind UT and is important to keep secret until we patent it or establish that it's not patentable (tho i think it is)}

*Ultra Terminum* is not a method to scale a single blockchain. It is a method to scale a *network* of blockchains. Those blockchains use the technique *PoW Reflection* to *mutually secure each other*, forming something called *the simplex*. Blockchains at this level are called *simplex-chains* and are able to host *dapp-chains* -- chains that are dedicated to a dapp, and they can be application specific (e.g. a DEX) or general (e.g. a chain hosting an instance of the EVM).

Practically, there is little difference between a single blockchain and the simplex. PoW reflection allows each simplex-chain, and each hosted dapp-chain, to do efficient SPV proofs regarding state-entries on other simplex-chains or dapp-chains. This allows for the creation of rich cross-chain functionality, such as the ability to use a single network-level token across all simplex-chains and dapp-chains.

Although SPV proofs can be hundreds of bytes long, UT can be structured so that the length of these proofs does not have a large impact on scalability. UT does this by prioritizing a clean and simple protocol for simplex-chains, and allowing dapp-chains the freedom to implement whatever state- and transaction-protocols are suitable for the application in question. This means that complex and inefficient data-structures for a dapp-chain's state do not impact the network globally, preventing bottlenecks in foundational layers.

\begin{comment}
\begin{figure}
\centering
\includegraphics[width=80mm]{ut/tiling/d1-many-tiled-17-simplexes}
\caption{A graph of a 17-chain simplex. Vertices are simplex-chains. Edges are the reflections between simplex-chains.}
\label{fig:tiled-simplex-17-d1}
\end{figure}
\end{comment}
