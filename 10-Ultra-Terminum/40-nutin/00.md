## Nos Ultra Terminum Ultimum Imus, Nunc

\todo[inline]{**note: this section contains the innovations behind UT and is important to keep secret until we patent it or establish that it's not patentable (tho i think it is)**}

\todo[inline]{Skip forward to "PoW Reflection"}

**outdated, not accurate**

note: each chain on the MC will use the merkle branch to verify PoW.
the mc itself doesn't have state.
so the same mc proof might have a valid bitcoin ext but not a valid NMC ext.
how to fix?
what about if *the merged* BTC and NMC do PoW reflection?
- then, their *reward* but not the work+txs are contingent on the PROOF of their block being *also* used in an NMC block, IFF they mined a merged block.
(if they didn't include an NMC update then obvs they can't lose a reward for that work not ending up in NMC chain???)
(mb, by default, a nonexistent update is an empty update?)
(does that make things way too complex? well it doesn't point back to anything so that's a problem right)
(but it could be implicitly included in all 'fuller' NCM blocks/transitions)
(how does reflection work for empty stuff? no new block headers are included)

**outdated, not accurate**

EVERY block must build on the 'external reflection' chain -- which operates as a 'light client' for all ext microchain-ext-reflection chains

**outdated, not accurate**

each blockchain gets, for each MicroBlock:

- header + proof for it
- chainId for all other chains
- ...

### Sharing work with PoW Reflection - O(c^2) scalability

(Mutual Confirmation of Confirmation)

### Heterogeneous PoW

### Construction of Ultra Terminum

#### Complexity Analysis

**outdated, not accurate**

- M microchains -> O(M) ~ O(1)
- O(M^2) ~ O(1) for M microchains w/ reflection
- each microchain supports O(c) chains
- each chain supports O(c)
- O(c^2) scaling with O(n) security

is the total security O(n) or O(cn) or something else?


### searching for prior art, notes

search terms that didn't find anything

* securely sharing pow between blockchains
* generalized merged mining
